import endpoint from "../../endpoint.json";

export const AiEndpoint = `http://${endpoint.AI.ip}:${endpoint.AI.port}`;
export const yukarinetEndpoint = `ws://${endpoint.yukarinet.ip}:${endpoint.yukarinet.port}/textonly`;
